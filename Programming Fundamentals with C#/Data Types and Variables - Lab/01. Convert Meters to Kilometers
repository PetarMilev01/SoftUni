using System;

    public class Program
    {
        static void Main(string[] args)
        {
            double meters = double.Parse(Console.ReadLine());
            double kms = meters / 1000;
            Console.WriteLine($"{kms:0.00}");
        }
    }
